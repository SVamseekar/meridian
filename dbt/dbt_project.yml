name: 'meridian'
version: '1.0.0'
config-version: 2

profile: 'meridian'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  meridian:
    staging:
      +materialized: view
      +schema: staging
